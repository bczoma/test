name: Test exiting on failure

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Try to fail
      run: exit 1
    - name: Print message
      if: ${{ failure() }}
      run: echo FAILED!

